# =================================================
# DEV ENVIRONMENT VARIABLES
# This file is for LOCAL DEVELOPMENT ONLY.
# Safe to store in git repository.
# =================================================

# MySQL root password (simple, for local dev only)
MYSQL_ROOT_PASSWORD=root
# Database schema name (matches migration scripts)
MYSQL_DATABASE=dniester
# Dev-time app user (optional)
MYSQL_USER=devuser
MYSQL_PASSWORD=devpass

# Spring Boot application ports
SPRING_LOCAL_PORT=8080
DATABASE_LOCAL_PORT=3306

# Spring Boot datasource config (dev uses root account)
SPRING_DATASOURCE_URL=jdbc:mysql://mysql:3306/dniester?useUnicode=true&characterEncoding=utf8mb4&useSSL=false
SPRING_DATASOURCE_USERNAME=root
SPRING_DATASOURCE_PASSWORD=root

# Application admin credentials (dev only, weak on purpose)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin