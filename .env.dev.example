# =================================================
# DEV ENVIRONMENT VARIABLES (EXAMPLE TEMPLATE)
# Copy this file to .env.dev and adjust values for local development.
# DO NOT COMMIT your real .env.dev, only this template belongs in git.
# =================================================

# MySQL root password (for local dev only)
MYSQL_ROOT_PASSWORD=root
# Database schema name
MYSQL_DATABASE=dniester
# Optional app db user
MYSQL_USER=devuser
MYSQL_PASSWORD=devpass

# Ports
SPRING_LOCAL_PORT=8080
DATABASE_LOCAL_PORT=3306

# Spring Boot datasource config (dev uses root by default)
SPRING_DATASOURCE_URL=jdbc:mysql://mysql:3306/dniester?useUnicode=true&characterEncoding=utf8mb4&useSSL=false
SPRING_DATASOURCE_USERNAME=root
SPRING_DATASOURCE_PASSWORD=root

# Application admin credentials (dev only)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin