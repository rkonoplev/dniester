# =================================================
# Example .env file for local development
# -------------------------------------------------
# Copy this file to ".env" and adjust values for
# your local environment.
# This file SHOULD be committed to Git.
# =================================================

# =============================
# MySQL Database (docker-compose)
# =============================
MYSQL_ROOT_PASSWORD=changemeRoot        # Root password for MySQL container
MYSQL_DATABASE=newsdb                   # Schema used by the application
MYSQL_USER=newsuser                     # Application DB username
MYSQL_PASSWORD=changemePass             # Application DB password

# =============================
# Ports
# =============================
SPRING_LOCAL_PORT=8080                  # Spring Boot port mapping
DATABASE_LOCAL_PORT=3306                # MySQL exposed port for localhost access

# =============================
# Spring Boot Datasource
# =============================
# Important:
# - hostname "localhost" works for local IDE runs.
# - hostname "mysql" works inside docker-compose.
SPRING_DATASOURCE_URL=jdbc:mysql://mysql:${DATABASE_LOCAL_PORT}/newsdb?useUnicode=true&characterEncoding=utf8mb4&useSSL=false
SPRING_DATASOURCE_USERNAME=${MYSQL_USER}
SPRING_DATASOURCE_PASSWORD=${MYSQL_PASSWORD}

# =============================
# Application bootstrap
# =============================
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changemeAdmin

# -------------------------------------------------
# Notes:
# - Replace "changeme..." defaults with real values in ".env".
# - Never commit real secrets to GitHub.
# - For production (Render), define secrets in Render dashboard,
#   do not copy SECRET values from here.
# -------------------------------------------------